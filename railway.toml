[build]
builder = "NIXPACKS"
buildCommand = "npm run db:migrate && npm run build"

[deploy]
startCommand = "npm run db:migrate && npm run seed && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
